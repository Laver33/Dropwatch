

model User {
  id        Int      @id @default(autoincrement())
  telegramId Int?    @unique
  email     String   @unique
  password  String
  
  plan      Plan     @default(FREE)
  products  Product[]
}

enum Plan {
  FREE
  BASIC
  PREMIUM
}