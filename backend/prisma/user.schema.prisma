

model User {
  id        String      @id @default(cuid())
  telegramId Int?    @unique
  email     String   @unique
  password  String
  
  plan      Plan     @default(FREE)
  products  Product[]
}

enum Plan {
  FREE
  BASIC
  PREMIUM
}